{
  "name": "caronutriholistica-evaluacion",
  "version": "1.0.0",
  "private": true,
  "description": "Landing de evaluación de síntomas (quiz NutriBalance). Sitio estático que envía todas las respuestas al webhook de n8n.",
  "scripts": {
    "config": "node scripts/render-config.mjs",
    "dev": "npm run config && node scripts/dev-server.mjs",
    "start": "node scripts/dev-server.mjs",
    "docker:build": "docker compose build",
    "docker:up": "docker compose up -d --build",
    "docker:down": "docker compose down"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "UNLICENSED"
}
